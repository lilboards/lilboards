{
  "rules": {
    "boards": {
      "$board_id": {
        "board": {
          ".read": "true",
          ".write": "auth.token.email_verified === true"
        },

        "columns": {
          ".read": "auth.uid !== null",
          ".write": "auth.uid !== null"
        },

        "items": {
          ".read": "auth.uid !== null",
          ".write": "auth.uid !== null"
        }
      }
    },

    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
