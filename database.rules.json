{
  "rules": {
    "boards": {
      "$board_id": {
        "board": {
          ".read": "auth != null",
          ".write": "auth.token.email_verified === true"
        },

        "columns": {
          ".read": "auth.token.email_verified === true",
          ".write": "auth != null"
        },

        "items": {
          ".read": "auth != null",
          ".write": "auth != null"
        }
      }
    },

    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
