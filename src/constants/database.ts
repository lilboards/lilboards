export enum DatabaseKey {
  board = 'board',
  boards = 'boards',
  columns = 'columns',
  items = 'items',
  itemIds = 'itemIds',
  likes = 'likes',
  list = 'list',
  lists = 'lists',
  rows = 'rows',
  users = 'users',
}
